---
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';

const title = `Página No Encontrada`;
---

<Layout metadata={{ title }}>
  <section class="flex items-center h-full p-8">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-lg text-center">
        <h2 class="mb-8 font-bold text-9xl">
          <span class="sr-only">Error</span>
          <span class="text-primary">404</span>
        </h2>
        <p class="text-3xl font-semibold md:text-3xl">
          ¡Oh no! Parece que esta página se ha perdido en el camino al altar.
        </p>
        <p class="mt-4 text-lg text-muted dark:text-slate-100">
          No te preocupes, puedes regresar y seguir disfrutando de nuestra celebración en la página principal.
        </p>

        <div class="flex flex-col items-center justify-center">
          <div class="relative mt-4 mb-8">
            <img
              src="/assets/images/wedding-invitation.gif"
              alt="Wedding Logo"
              width="200"
              height="200"
              class="inline"
            />
          </div>
        </div>

        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn ml-4">Regresar a la página principal</a>
      </div>
    </div>
  </section>
</Layout>
